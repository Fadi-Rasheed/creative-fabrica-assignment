import { TopActiveCreators } from '..'

describe('TopActiveCreators index component exports', () => {
  it('should be a function', () => {
    expect(TopActiveCreators).toStrictEqual(expect.any(Function))
  })
})
